apiVersion: v1
kind: PersistantVolumeClaim
metadata:
  name: db-pvc
  namespace: db-namespace
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: db-pv-class
  resources: 
    requests:
      storage: 5Gi  